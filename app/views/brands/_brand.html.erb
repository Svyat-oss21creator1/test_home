<div id="<%= dom_id brand %>">
  <h2>
    <strong>Brand:</strong>
    <%= brand.name %>
  </h2>

  <% @brand.models.each do |model|%>
  <p>
   <%= model.name%> <%= link_to"View Model", model%>
  </p>

  <% end %>

  <p>
    <strong>Description:</strong>
    <%= brand.description %>
  </p>

</div>
